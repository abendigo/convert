{
  "name": "convert",
  "repository": "abendigo/convert",
  "license": "Unlicense",
  "scripts": {
    "build": "copy-node-modules . dist && copyfiles *.html *.js manifest.json dist"
  },
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.2.4"
  },
  "devDependencies": {
    "copy-node-modules": "^1.1.1",
    "copyfiles": "^2.1.0"
  }
}
